import 'package:flutter/material.dart';

class ProfileSkills extends StatelessWidget {
  const ProfileSkills({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Skills Yay!'),
    );
  }
}