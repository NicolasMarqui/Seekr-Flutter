import 'package:flutter/material.dart';

class ProfileExperience extends StatelessWidget {
  const ProfileExperience({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Why the error though'),
    );
  }
}