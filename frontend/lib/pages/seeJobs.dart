import 'package:flutter/material.dart';

class SeeJobs extends StatelessWidget {
  const SeeJobs({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        child: Center(child: Text("Sup Fam"),),
      ),
    );
  }
}